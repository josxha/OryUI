@using global::Ory.Kratos.Client.Model
@code {
    [Parameter] public required List<KratosUiText>? list { get; set; }
}

@if (list != null)
{
    foreach (var message in list)
    {
        <KratosUiTextMessageComponent uiText="@message" />
    }
}